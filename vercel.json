{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "corepack enable && pnpm install --no-frozen-lockfile",
  "buildCommand": "corepack enable && cd artifacts/directorio-cartago && corepack pnpm run build",
  "outputDirectory": "artifacts/directorio-cartago/dist"
}
