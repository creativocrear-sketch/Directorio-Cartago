{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "corepack enable && pnpm --filter @workspace/directorio-cartago run build",
  "outputDirectory": "artifacts/directorio-cartago/dist"
}
